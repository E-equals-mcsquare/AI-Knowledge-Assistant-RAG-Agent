apiVersion: v1
kind: Service
metadata:
  name: ai-knowledge-assistant-service
  labels:
    app: ai-knowledge-assistant

spec:
  type: LoadBalancer

  selector:
    app: ai-knowledge-assistant

  ports:
  - name: http
    port: 80
    targetPort: 8000
